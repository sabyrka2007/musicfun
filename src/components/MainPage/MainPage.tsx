import { PageTitle } from '../PageTitle'
import { MainContent } from '../MainContent'

export const MainPage = () => {
  return (
    <main>
      <PageTitle />
      <MainContent />
    </main>
  )
}